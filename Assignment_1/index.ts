// Is there an alternative method available to transpile our TypeScript code without executing the tsc command?

//--------------------------------------------------------------------------

console.log("Hello Word!");

/*1) First you have to install (npm i ts-node -D).

2)  There is also another way write
    (tsc && node filename) or (npx ts-node filename).

3)In the terminal and enter 
It will transpole and run the file in single command*/